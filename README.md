# PayNest – Smart Installment Manager

Final consolidated build. Six runtime files only:

- index.html
- style.css
- app.js
- manifest.json
- sw.js
- README.md

Core data is shared through one in-app data model and persisted to LocalStorage, with optional Firebase Authentication + Firestore sync. UI/business logic is kept in app.js; CSS is presentation only.
